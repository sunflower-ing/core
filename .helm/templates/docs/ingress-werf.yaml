{{- if .Values.werf -}}
{{- $fullName := include "sunflower.fullname" . -}}
{{- $svcPort := .Values.docs.service.port -}}
{{- if and .Values.docs.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.docs.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.docs.ingress.annotations "kubernetes.io/ingress.class" .Values.docs.ingress.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "sunflower.labels" . | nindent 4 }}
  {{- with .Values.docs.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: nginx-external
  tls:
    - hosts:
        - "{{ .Values.envUrl }}"
      secretName: fakesecret
  rules:
    {{- range .Values.docs.ingress.hosts }}
    - host: "{{ $.Values.envUrl }}"
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-docs
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}-docs
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
        {{- end }}
  {{- end }}
